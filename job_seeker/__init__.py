"""job-seeker — top-level CLI orchestrator.

Currently provides `init` (scaffold private resume dirs + experience bank +
smoke compile). Future phases add `tailor` (end-to-end JD flow), `ats-check`,
and `templates` (registry). See docs/ROADMAP_v0.4.md.
"""
__version__ = "0.4.0-dev"
